class Theory{
public static void main(String[] args){
 byte ramofRedme=8;
 byte ramofVivo=4;
 byte ramofApple=16;
 byte ramofOneplus=6;
 System.out.println("the ram size of the Redme is:"+ramofRedme);
 System.out.println("the ram size of the Vivo is:"+ramofVivo);
 System.out.println("the ram size of the Apple is:"+ramofApple);
 System.out.println("the ram size of the Oneplus is:"+ramofOneplus);
 
 
 }
 }
 variable is a that name given to the address.
 datatypes is a data that variable refers to.
 datatypes have 2 types 
 namely 
 1.primitive data types 
 2.object oriented references
 
 primitive data types is a predifine data that has a fixed size
 
1.byte=1byte it contains 8 bits=-128to127
2.short= 2 bytes it contains =16 bits=-32568to 32567
3int=4 bytes it contains =32 bits=-2999999999 to 2999999999
4.long=8 bytes it contains=64 bits=
5.char=2bytes=16bits=0 to 65355
6.float=4bytes=32bit 6 to 7
7.double=8bytes=64 bits=15 to 16
8.boolean=1 byte=8 bits=true or false

Object oriented references data types is a not a predefined data 
 that has no fixed size
 
 1.string
 2.array
 3.object custom classes
 
 
 = eguals to assign the values
 ==  eguals to compare the memory address
 
 
 local variable which is variable that is declared  inside a method ,
                  construction or a block that only assessed in the scope
 static variable
 non static variable
 parameterized variable
 
 
 1.Method overriding occurs when a child class provides a new implementation of a method already defined in its parent class.
 2.Method overloading means having multiple methods with the same name but different parameters
 3.Constructor overloading occurs when a class has multiple constructors with different parameter lists.
 4.Inheritance is in which inherits the proprties of one class to anpther class
 5.a constructor is a special method that is used to initialize objects of a class. It has the same name as the class and does not have a return type,
 
 | Concept                     | Meaning                                         | When it Happens        | Keyword/Condition          | Example                                   |
| --------------------------- | ----------------------------------------------- | ---------------------- | -------------------------- | ----------------------------------------- |
| **Method Overriding**       | Redefining parent method in child class         | Runtime                | Inheritance (`extends`)    | `Dog extends Animal`                      |
| **Method Overloading**      | Same method name, different parameters          | Compile-time           | Same class                 | `add(int, int)` and `add(double, double)` |
| **Constructor Overloading** | Multiple constructors with different parameters | Compile-time           | Same class                 | `Student()` and `Student(String)`         |
| **Inheritance**             | One class inherits another                      | Compile-time           | `extends`                  | `Car extends Vehicle`                     |
| **Polymorphism**            | One object behaves in many forms                | Runtime & Compile-time | Parent reference for child | `Animal a = new Dog()`                    |

 
 
 
 
 
 
 
 
 
 
 
 
 
 
 